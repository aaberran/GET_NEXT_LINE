# GET_NEXT_LINE
Get Next Line - 42 Project  A C function that reads and returns one line at a time from a file descriptor.  Handles multiple file descriptors, manages memory efficiently, and demonstrates  the use of static variables for state preservation between function calls.
